rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userEmail} {
      allow read: if request.auth.uid == userEmail;
    }
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}